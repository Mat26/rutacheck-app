import Screen from "@shared/components/Screen";
import { Text } from "react-native";

export default function ReminderWebPlaceholder() {
  return (
    <Screen>
      <Text>La función de recordatorios está disponible en la app móvil.</Text>
    </Screen>
  );
}
